19c17
<     cursor.execute(f"SELECT * FROM sqli1_users WHERE password = '{password}' AND username = '{username}'")
---
>     cursor.execute(f"SELECT * FROM sqli1_users WHERE password = %s AND username = %s", (password, username))
